{
  "buildCommand": "npm run build -w miniapp",
  "outputDirectory": "apps/miniapp/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
