{
  "buildCommand": "npm run build --workspace=apps/miniapp",
  "outputDirectory": "apps/miniapp/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
